import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	list: {
		flex: 1,
		gap: 4
	},
	placeholder: {
		alignItems: "center"
	},
	sticker: {
		transform: [{ rotate: "0deg" }],
		width: 100,
		height: 120
	}
});
